# Only numbers that are NOT derivable from this repo live here.
#
# The CVE count is deliberately absent: it is computed in section-metrics.html
# as `site.cves | size`, so it can never disagree with the CVE list.
#
# CONFIRM: 700 is derived from the CTF_writeups repo (~710 markdown files
# carrying frontmatter, minus index pages), not supplied. Delete or correct it.

- label: CTF writeups published
  value: 700
  suffix: "+"
  icon: bi-journal-code

# Add once you have the figures, e.g.:
# - label: Bug bounty reports resolved
#   value: 0
#   suffix: ""
#   icon: bi-shield-check
